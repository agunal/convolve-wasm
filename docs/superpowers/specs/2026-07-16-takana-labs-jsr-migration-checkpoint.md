# Migration checkpoint

The branch intentionally begins with a failing identity contract. Implementation follows only after pull-request CI demonstrates the legacy `@agunal`/npm configuration violates the approved Takana Labs and JSR design.
